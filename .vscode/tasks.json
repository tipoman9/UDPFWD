{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gcc build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "-o",
                "${workspaceFolder}/udpfwd",
                "${workspaceFolder}/udpfwd.c",
                "-levent",
                "-levent_core"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
